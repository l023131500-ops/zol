/**
 * @kesef/metrics — derived metrics, peer groups, alert rules (Wave 3).
 *
 * metric_value per authority/year with a user-visible `formula` (task 25),
 * peer-group computation (task 26) and the 12 alert rules whose statement_he
 * is checked against the forbidden-wording list in 05_GUARDRAILS.md (task 27).
 */
export const METRICS_PLACEHOLDER = true;
